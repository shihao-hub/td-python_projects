[project]
name = "flet-android-lab"
version = "0.1.0"
description = "Flet lab: build Android apps in pure Python (VS Code + Flet)"
readme = "README.md"
authors = [
    { name = "shawn.zhang", email = "shawn.zhang@tec-do.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flet>=0.28.0",
    "structlog>=26.1.0",
]
