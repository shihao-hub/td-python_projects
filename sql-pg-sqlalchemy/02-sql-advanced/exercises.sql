-- ============================================================
-- 阶段 2 练习：子查询 / CTE / 窗口函数（共 10 题）
-- 全部只读。先自己写，再对照 solutions.sql。
-- ============================================================

-- ---------- 题 1（标量子查询） ----------
-- 用相关子查询查询用户 6、23、34 的：昵称、订单数、首单时间。
-- （标量子查询写法；想想这个查询在 100 万订单上跑起来什么感觉——阶段 4 会回头看它的执行计划）

-- ---------- 题 2（IN 与 EXISTS 双写） ----------
-- 统计「2026 年 9 月下过单的用户数」。
-- 分别用 IN 子查询和 EXISTS 相关子查询各写一遍，结果应一致（约 1.2 万）。

-- ---------- 题 3（NOT IN 陷阱 + NOT EXISTS） ----------
-- 3a. 先在 psql 里跑这两个，理解 NULL 陷阱：
--     SELECT 1 WHERE 3 NOT IN (1, 2);
--     SELECT 1 WHERE 3 NOT IN (1, NULL);
-- 3b. 「从未被购买过的商品」数量：分别用 NOT IN 和 NOT EXISTS 写，结果应一致。
--     （子查询指向 order_items 的 product_id。）

-- ---------- 题 4（相关子查询 vs 窗口函数） ----------
-- 「每个分类最贵的商品」：分类名、商品名、价格。
-- 相关子查询写一遍；窗口函数（rank）写一遍。价格并列时两版行为可能不同，思考一下为什么。

-- ---------- 题 5（CTE） ----------
-- 2026 年每个月的订单数、总金额，按月排序。
-- 输出列：month（YYYY-MM 格式）、order_cnt、amount。用 CTE + date_trunc。
-- 预期：9 行（2026-01 ~ 2026-09）。

-- ---------- 题 6（递归 CTE） ----------
-- 从分类「手机数码」(id=1) 出发，列出它和它所有子孙分类，输出：id、name、depth（顶级=0）。
-- 预期：3 行（手机数码 + 手机 + 智能穿戴）。
-- 附加：把答案里 WHERE id = 1 改成 WHERE id IS NULL 换个起点试试全树。

-- ---------- 题 7（每组 Top-N） ----------
-- 每种订单状态「金额最大」的 3 笔订单：status、id、total。
-- 要求用窗口函数实现，最终结果按 status、金额降序。
-- 预期：15 行（5 种状态 × 3）。

-- ---------- 题 8（累计） ----------
-- 最近 14 天（含今天）每天的订单数、以及从窗口第一天开始的累计订单数。
-- 输出列：day、day_cnt、running_cnt。提示：count(*) OVER (ORDER BY day)。

-- ---------- 题 9（LAG） ----------
-- 用户 42 的所有订单，按时间升序，输出：id、created_at、距上一单的小时数（保留 1 位小数，
-- 第一单该列为 NULL）。

-- ---------- 题 10（窗口总计 / 占比） ----------
-- 消费金额 Top 10 的用户：user_id、昵称、消费金额、占全体用户总消费的比例（百分数保留 2 位）。
-- 要求用窗口函数算总消费（sum() OVER ()），不要两个独立查询拼。
