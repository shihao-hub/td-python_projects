-- ============================================================
-- 阶段 1 练习：SQL 基础（共 15 题）
-- 用法：psql -U postgres -d learn_pg，然后 \i exercises.sql 逐段看题，
--       自己写 SQL 跑，最后对照 solutions.sql。
-- 注意：第 13~15 题会写入数据，做完会清理；其他题都是只读的。
-- ============================================================

-- ---------- 题 1（基础查询 + 排序） ----------
-- 查询 status = 'banned' 的用户，返回 id、email、nickname、created_at，
-- 按注册时间从早到晚排序，只取前 10 条。
-- 预期：恰好 10 行。

-- ---------- 题 2（组合条件 + 范围） ----------
-- 查询 9 号分类（手机）下，价格在 1000~3000 之间且有库存的商品，
-- 返回 name、price、stock，按价格从高到低排序。
-- 预期：几十行。

-- ---------- 题 3（IN / LIKE / IS NULL） ----------
-- 三个小查询：
-- 3a. status 为 'pending' 或 'cancelled' 的订单数量（一条 SQL，预期 120000）
-- 3b. 商品名以「小米」开头的商品数量（预期 400 左右）
-- 3c. tags 为 NULL 的商品数量（预期 250）

-- ---------- 题 4（聚合入门） ----------
-- 一条 SQL 统计 orders：总订单数、订单总金额、平均客单价（保留 2 位小数）、最早和最晚下单时间。

-- ---------- 题 5（GROUP BY） ----------
-- 按订单状态统计：每种状态的订单数、总金额，结果按订单数从多到少排序。
-- 预期：5 行，completed 的订单数应为 580000。

-- ---------- 题 6（HAVING） ----------
-- 查询下单数超过 500 的用户：user_id 和订单数，按订单数降序。
-- 取前 5 行即可。预期：都是 1000+ 单的 VIP 用户（见 setup 说明）。

-- ---------- 题 7（GROUP BY + JOIN） ----------
-- 每个商品分类的商品数、平均价格（保留 2 位小数）；
-- 只要叶子分类（parent_id 不为 NULL 的分类），结果按商品数降序。
-- 提示：GROUP BY 用 c.id, c.name。

-- ---------- 题 8（INNER JOIN） ----------
-- 最近 10 笔 pending 订单：订单 id、金额、下单时间、用户 email 和昵称。
-- 按下单时间倒序。

-- ---------- 题 9（LEFT JOIN 反连接） ----------
-- 统计「从来没有过 pending 订单」的用户数量。
-- 提示：LEFT JOIN pending 订单，右表 IS NULL；注意匹配条件写在 ON 里。

-- ---------- 题 10（多表 JOIN） ----------
-- 订单 id = 10086 买了哪些商品：商品名、数量、单价、小计（数量×单价，保留 2 位小数）。

-- ---------- 题 11（自连接） ----------
-- 列出所有叶子分类（parent_id 不为 NULL）的名称及其父分类名称，按子分类 id 排序。
-- 预期：12 行。

-- ---------- 题 12（JOIN + 聚合 + 子查询混搭） ----------
-- 消费总金额最高的前 5 个用户：user_id、昵称、总消费金额、订单数。
-- 预期：VIP 用户霸榜（人均约 1000 单）。

-- ---------- 题 13（INSERT + RETURNING） ----------
-- 插入一个测试用户：email='test_delete_me@example.com'，nickname='test_delete_me'，
-- 其他字段默认。用 RETURNING 拿到它的 id。
-- （做完记下 id，第 15 题删它。）

-- ---------- 题 14（UPDATE + F 表达式思想） ----------
-- 把「test_delete_me」这个用户所有 pending 订单改为 cancelled（正常它没有订单，0 行更新即可）；
-- 再写一条 UPDATE 把它的 nickname 改为 'test_delete_me_2'。

-- ---------- 题 15（DELETE + 外键约束） ----------
-- 删除 nickname 为 'test_delete_me_2' 的用户。
-- 先直接删 users 行试试——如果报外键冲突（取决于它有没有订单），思考为什么，然后正确清理。
