# AGENTS.md

## 仓库结构说明

本目录（`python_projects`）采用 **monorepo** 结构管理所有 Python 项目：

- 每个子目录都是一个独立的项目（例如 `demo-api`、`pystand-lab`），彼此互不依赖归属关系，各自维护自己的依赖与配置。
- 整个目录作为单一 Git 仓库进行版本管理，不再为单个项目单独建仓。
- 新增项目时直接在目录下创建新的子目录即可，不要在子项目内单独 `git init`。
- 在本目录下工作时，先确认目标项目所在子目录，再在该子目录的上下文中执行构建、测试等操作。
