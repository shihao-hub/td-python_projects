# 复制为 .env 后按需修改；全部有开发默认值，可不配
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=["localhost","127.0.0.1"]

# 默认 SQLite；切换 PostgreSQL（先 uv sync --group postgres）：
# DJANGO_DATABASE_URL=postgres://user:password@localhost:5432/django_lab
